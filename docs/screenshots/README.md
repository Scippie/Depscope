# Screenshots

Place the main application screenshot used by the root README at:

```text
docs/screenshots/depscope-main.png
```

Recommended capture: the main DepScope window after scanning a representative project, with package rows and vulnerability/update columns visible.
